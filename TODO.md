# French Schools Map — TODO

- [x] **README** — missing the 6 new `show_filter_*` attributes and `tile_url` in the attributes table — **done**
- [ ] **Admin default settings for all filters** — ability to set default `types`, `statut`, `ep` in the global settings (not just geography)
